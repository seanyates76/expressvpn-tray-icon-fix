# Original Asset Notes

`resources/original/` is for local extraction outputs from an installed
ExpressVPN client.

The public repo intentionally keeps only metadata such as:

- `manifest.txt`

Exact extracted vendor tray PNGs are not committed here. To populate them
locally, run:

```bash
make extract
```
